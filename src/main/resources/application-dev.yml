server:
  port: 8888

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/dbloja
    username: root
    password: senha
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
logging:
  level:
    net:
      ttddyy:
        dsproxy:
          listener: debug